import 'package:flutter/material.dart';

class Padd extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Padding(padding: const EdgeInsets.all(10));
  }

}